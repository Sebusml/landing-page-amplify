import React, { ReactElement } from "react";
import { GetStaticProps, GetStaticPaths } from "next";

interface Props {}

export default function IndividualPost({}: Props): ReactElement {
  return <div>TODO: post pages</div>;
}
